// quick google search for this solution.
function wordReversal(str) {
    return str.split("").reverse().join("");
}

// no print in JS, need to use console.log !
console.log(wordReversal("testword")) // drowtset